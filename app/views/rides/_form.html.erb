<%= form_for([current_user,@ride]) do |f| %>
  <% if @ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride.errors.count, "error") %> prohibited this ride from being saved:</h2>

      <ul>
      <% @ride.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :festival_id %><br />
    <%= f.collection_select :festival_id, Festival.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :leave_date %><br />
    <%= f.date_select :leave_date %>
  </div>
  <div class="field">
    <%= f.label :leave_date %><br />
    <%= f.date_select :leave_date %>
  </div>
  <div class="field">
    <%= f.label :return_date %><br />
    <%= f.date_select :return_date %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :city %><br />
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :state %><br />
    <%= f.text_field :state %>
  </div>
  
  <div class="field">
    <%= f.label :cost %><br />
    <%= f.number_field :cost %>
  </div>
  <div class="field">
    <%= label_tag(:giving_ride, "I'm giving a ride") %>
    <%= check_box_tag(:giving_ride) %>
  </div>
  <div class="actions">
    <%= f.submit 'Create' %>
  </div>
<% end %>
