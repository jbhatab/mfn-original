<h1>
  <%= @commentable.name%>
</h1>

<% @comments.each do |comment| %>
  <div class="comment-container">
    <header class="comment-header">
      <%= comment.title %>
    </header>
    <%=comment.comment%>
  </div>
<%end%>

